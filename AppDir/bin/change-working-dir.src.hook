dir=$HOME/.driver2
if [ ! -f "dir"/config.dat ]; then
  mkdir -p "$dir"
  cp "$APPDIR"/bin/config.ini "$dir"
  cp "$APPDIR"/bin/cutscene_recorder.ini "$dir"
fi
cd "$dir"

exec "$APPDIR"/bin/REDRIVER2 "$@"
